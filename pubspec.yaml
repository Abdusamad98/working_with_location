name: working_with_location
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  #Getting device location
  geolocator: ^9.0.1
  location: ^4.2.0
  geocoding: ^2.0.5

  google_maps_flutter: ^2.2.0

  fluttertoast: ^8.0.9
  intl: ^0.17.0
  dio: ^4.0.6
  sqflite: ^2.1.0
  provider: ^6.0.3
  json_serializable: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
